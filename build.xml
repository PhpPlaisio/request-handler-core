<project name="SetBased ABC-Framework" default="build" basedir=".">
    <!-- Run composer update and executes various other updates -->
    <target name="update">
        <exec command="composer update --prefer-source" checkreturn="true" passthru="true"/>
    </target>

    <target name="build">
        <echo message=""/>
    </target>
</project>
